# Mint Car Detail repaired patch

This version is based on the uploaded working `index(1).html` and keeps its existing:
- `./css/tailwind-build.css`
- `./css/index.css`
- `./assets/`
- Formspree form
- Google Analytics / Google Ads conversion code
- premium inline design CSS

## Replace these files
- root `index.html`
- root `index.js`
- city folders included in this ZIP

Keep your existing `css/`, `assets/`, and `scripts/` folders.

## Gallery
The gallery HTML is generated by `index.js`.
Add any number of image paths to the `GALLERY_IMAGES` array in `index.js`.
A missing image only removes that slide; it does not break the whole gallery.

## Cities
The root `/` page is general.
Real city endpoints are included:
- `/hamilton/`
- `/burlington/`
- `/ancaster/`
- `/waterdown/`
- `/dundas/`

`index.js` also reads the URL and applies the correct city content.
